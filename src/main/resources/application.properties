# Application name used for identification in logs and monitoring
spring.application.name=BankLoanApplication

# JDBC driver class for MySQL database connections
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Database connection URL (connects to MySQL on localhost using 'infracred' database)
spring.datasource.url=jdbc:mysql://localhost/infracred

# HTTP server port (application accessible at http://localhost:8080)
server.port=8080

# Database authentication credentials
spring.datasource.username=root
spring.datasource.password=ibvl

logging.level.org.apache.catalina.core=OFF
logging.config=classpath:log4j2.xml


spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=pallavimane224@gmail.com
spring.mail.password=efdcilydstvufvpy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


